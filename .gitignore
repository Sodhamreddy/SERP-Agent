# Build / packaging artifacts (do NOT commit — these bloated the repo to GBs)
build/
dist/
SERP-Agent-Portable/
*.zip
*.exe
# Old Playwright bundle (removed)
browsers/
browser_profile/

# Python
__pycache__/
*.pyc

# Runtime data & secrets (auto-seeded / created at runtime — keep out of git)
config.json
users.json
tickets.json
clients.json
keywords.json
serp_agent.log
results/*.xlsx
results/*.csv
results/_progress*.json

# Generated product docs / decks — local only, never pushed
Excelliq_Product_Docs/
SERP_Agent_Product_Docs/
*.pdf
